/* global describe, it */
const { expect } = require("chai");

require("../src");

describe("computeConsoleTable", () => {
    it("... should correctly import @prbpad/pkg-b and console.log the table", () => {
        // importing the `src` folder suffices, there should be no error thrown there
        expect(true).to.equal(true);
    });
});
